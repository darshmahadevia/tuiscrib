# Build a keyboard-only Bun and OpenTUI React client

The terminal client uses React-bound OpenTUI in a Bun workspace monorepo and ships standalone macOS, Linux, and Windows binaries for Unicode 256-color terminals, enhancing truecolor when detected. Its explicit Navigate and Edit modes are keyboard-only, and one opaque session token persists in the protected platform config directory.
