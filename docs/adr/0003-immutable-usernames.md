# Keep usernames immutable

Each user chooses one service-wide unique username that cannot be renamed. This trades rename flexibility for stable human identity and unambiguous attribution across boards.
